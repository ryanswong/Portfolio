---
export interface Props {
  title?: string;
  id: string;
  addContainerStyles?: string;
}
const { title, id, addContainerStyles } = Astro.props;
---

<!-- FIXME can i simplify this? -->

<!-- <section> -->
  <div id={id} class=`${addContainerStyles} scroll-mt-20 flex justify-center mt-16 mb-48 md:mt-0`>
    <div class="max-w-screen-lg flex flex-col items-center mx-10">
      {
        title ? (
          <h2 class="mb-16 text-5xl font-semibold m-auto">{title}</h2>
        ) : null
      }
      <slot />
    </div>
  </div>
<!-- </section> -->
